{% set classes = [
    'block-profile',
    'container',
] %}

<section{{ attributes.addClass(classes) }}>
    {% include '@enviedessence/component/_underline-title.html.twig' with { title: content.field_title, other: true } %}
    <div class="profile">
        <div
            data-aos="fade-right"
            data-aos-duration="1500"
            class="profile__picture"
        >
            {{ content.field_image }}
        </div>
        <div
            data-aos="fade-left"
            data-aos-duration="1500"
            class="profile__content"
        >
            <div class="text">
                {{ content.field_text }}
            </div>
            <div class="signing">
                {{ content.field_signing }}
            </div>
        </div>
    </div>
</section>
