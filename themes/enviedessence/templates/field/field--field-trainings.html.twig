<div{{ attributes.addClass('field-trainings') }}>
    {% for item in items %}
        <div
            class="training-wrapper"
            data-aos="fade-up"
            data-aos-duration="1000"
            data-aos-delay="{{ 300 * loop.index0 }}"
        >
            {{ item }}
        </div>
    {% endfor %}
</div>
